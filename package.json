{
  "name": "tts-mcp-server",
  "version": "1.0.0",
  "description": "TTS interface MCP server",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@chatmcp/sdk": "^0.0.18",
    "axios": "^1.6.0"
  }
}
